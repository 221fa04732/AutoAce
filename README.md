## 🚗 AutoAce

### 🛠️ Local Setup For Dashboard

1. **Select folder**
    - Go to dashboard folder
    ```bash
    cd dashboard

2. **Install dependencies**
    ```bash
    npm install

3. **Setup environment variables**
    - Create a .env file in the root directory

    - Copy all variables from .env.example

    - Fill in the required credentials

4. **Run the application**
    ```bash
    npm run dev


### 🛠️ Local Setup For Webhooks

1. **Select folder**
    - Go to webhooks folder
    ```bash
    cd webhooks

2. **Install dependencies**
    ```bash
    npm install

3. **Setup environment variables**
    - Create a .env file in the root directory

    - Copy all variables from .env.example

    - Fill in the required credentials

4. **Run the application**
    ```bash
    npm start


### 🛠️ Local Setup For LLM_Assistent

1. **Select folder**
    - Go to llm_assistent folder
    ```bash
    cd llm_assistent

2. **Install dependencies**
    ```bash
    npm install

3. **Setup environment variables**
    - Create a .env file in the root directory

    - Copy all variables from .env.example

    - Fill in the required credentials

4. **Run the application**
    ```bash
    npm start